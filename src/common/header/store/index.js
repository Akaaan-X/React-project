import reducer from './reducer';
import * as actionCreators from './actionCreators';
import * as constants from './constants';

export {reducer, actionCreators, constants}   //为了方便路径更加简洁，引入时会自动找store文件夹下的所有内容